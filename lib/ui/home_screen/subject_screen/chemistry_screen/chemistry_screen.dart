import 'package:flutter/material.dart';
class ChemistryScreen extends StatelessWidget {
  const ChemistryScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('Chemistry Screen'),),
    );
  }
}
