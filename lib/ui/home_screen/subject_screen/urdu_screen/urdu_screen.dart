import 'package:flutter/material.dart';
class UrduScreen extends StatelessWidget {
  const UrduScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('Urdu Screen'),),
    );
  }
}
